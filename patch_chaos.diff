<<<<<<< SEARCH
    const validatePayload = (qrData: string) => {
      let payload: unknown;
      try {
        payload = JSON.parse(qrData);
      } catch (jsonErr) {
        return false;
      }
      if (!payload || typeof payload !== 'object') return false;

      // Type checking constraints
      if (!payload.userId || typeof payload.userId !== 'string') return false;
      if (!payload.eventId || typeof payload.eventId !== 'string') return false;
      if (!payload.registrationId || typeof payload.registrationId !== 'string') return false;
=======
    const validatePayload = (qrData: string) => {
      let payload: { userId?: string; eventId?: string; registrationId?: string } | null = null;
      try {
        payload = JSON.parse(qrData) as { userId?: string; eventId?: string; registrationId?: string };
      } catch (jsonErr) {
        return false;
      }
      if (!payload || typeof payload !== 'object') return false;

      // Type checking constraints
      if (!payload.userId || typeof payload.userId !== 'string') return false;
      if (!payload.eventId || typeof payload.eventId !== 'string') return false;
      if (!payload.registrationId || typeof payload.registrationId !== 'string') return false;
>>>>>>> REPLACE
<<<<<<< SEARCH
  describe('5. Stripe Webhook Decoupling (Firestore Rules Validation)', () => {
    const checkSecurityRules = (auth: { uid: string; role: string } | null, resourceData: unknown, targetEvent: typeof mockEventData | typeof mockPremiumEventData) => {
      // Rule translation check
      const isAuthenticated = auth !== null;
      if (!isAuthenticated) return false;

      // Standard user matching check
      if (auth.role === 'STUDENT' && resourceData.userId !== auth.uid) return false;
=======
  describe('5. Stripe Webhook Decoupling (Firestore Rules Validation)', () => {
    const checkSecurityRules = (auth: { uid: string; role: string } | null, resourceData: { userId?: string } | unknown, targetEvent: typeof mockEventData | typeof mockPremiumEventData) => {
      // Rule translation check
      const isAuthenticated = auth !== null;
      if (!isAuthenticated) return false;

      // Standard user matching check
      if (auth.role === 'STUDENT' && (resourceData as { userId?: string }).userId !== auth.uid) return false;
>>>>>>> REPLACE
